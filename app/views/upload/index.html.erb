<div class="content">
  <p>Upload a file using the form or you may like to <%=link_to "upload using FTP", :action => 'ftp'%>. Only Photos that have been geo-tagged will be displayed on the map. Currently there is no way to geotag them after upload. </p>
  <p>Once you have finished <%= link_to "head over to your homepage", :controller => 'homepage' %> to see the photos you have uploaded.</p>
  <% form_tag( { :controller => 'upload', :action => 'index' }, :method => 'post', :multipart => true) do %>
      <%=file_field_tag 'file' %>
      <%= submit_tag "Upload" %>
  <% end %>
</div>
<%= render :file => 'content/legals' %>
